package Onyx;

import java.awt.Color;

/**
 * A simple class to hold a custom colours,
 * I purposefully used the British spelling to keep it
 * separate from Java's builtin class. Use "blu" as a template
 */
public class Colours {
    public static Color blu = new Color(0.1f, 0.1f, 0.5f);
}
